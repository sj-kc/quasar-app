export const SET_DEN_STORE = (state, denStore) => {
  state.stores = denStore;
};

export const SET_LOADED = (state, loaded) => {
  state.loaded = loaded;
};

export const SET_ERROR = (state, error) => {
  state.error = error;
};
